import style from '../modules/Banner.module.css';

const Banner = () => {
    return (
        <main>
            <section className={style.banner}>
                <h1>Bienvenue</h1>
            </section>
        </main>
    )
};

export default Banner;